name: hospital_finder_app
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.2.3
  flutter_rating_bar: ^4.0.1
  geocoding: ^2.1.1
  url_launcher: ^6.1.11
  cached_network_image: ^3.2.3
  firebase_core: ^4.0.0
  firebase_auth: ^6.0.0
  cloud_firestore: ^6.0.0
  provider: ^6.1.1
  geolocator: ^14.0.2  # Correct version
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  # flutter_dotenv: ^5.0.2  # Optional for now
flutter:
  assets:
    - assets/local_hospitals.json

  uses-material-design: true
